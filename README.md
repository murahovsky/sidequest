# Sidequest

Your main quest is the code. The spinner is the side quest.

While Claude works, instead of `Pondering…` you get one-line facts about a topic **you** picked — octopuses, ancient Rome, jazz, the Soviet space program — written in **your** language, generated by **your own Claude**. No server, no ads, no telemetry.

```
✻ Saturn would float if you dropped it in a big enough ocean… (4s · esc to interrupt)
```

## Install

One command (requires Node.js and Claude Code):

```sh
npx github:murahovsky/sidequest
```

That's the whole product: `sidequest` installs the plugin on first run and drops you into a Claude session that asks what topic fascinates you. Pick one — facts appear in the spinner within seconds and keep rotating, **a new one every ~3 seconds, even mid-thinking**.

From then on, launch Claude through `sidequest` — all `claude` arguments pass through:

```sh
sidequest                 # instead of claude
sidequest -c              # instead of claude -c
alias claude=sidequest    # optional: make it the default
```

If you launch plain `claude` instead, facts still show — just one fresh fact per message rather than a live stream (Claude Code samples the spinner text once per message; that's the platform ceiling without a wrapper).

## Commands

| Command | What it does |
|---|---|
| `/sidequest:topic <topic>` | Pick or change the topic |
| `/sidequest:topic surprise me` | Eclectic mix across science, history, nature, art |
| `/sidequest:off` | Turn facts off, restore your previous spinner |

## How it works

- Claude Code has a documented setting for the spinner verb line: `spinnerVerbs`. It hot-reloads — changes apply mid-session, no restart. The tips line is left untouched.
- On setup, a headless model (`claude -p --model sonnet`) writes the first facts straight into the spinner — live in ~10 seconds — then a detached background job grows the pool in `~/.claude/sidequest/facts.json`. Your main session never composes facts, so setup stays fast even on heavyweight models; generation is the only step that costs tokens.
- Claude Code samples the spinner verb **once per message** (verified empirically — settings hot-reload updates the list, never the text already on screen). So hooks rotate a fresh shuffled batch into `~/.claude/settings.json` on every session start and message: one new fact per message, zero extra processes.
- The `sidequest` wrapper removes that limit at the display layer: it sets the verb to a 64-column sentinel token and, sitting on a pty between your terminal and Claude Code, substitutes the token in the output stream with the current fact — tracking cursor columns so even Ink's partial repaints stay consistent. The fact swaps every ~3 seconds (`SIDEQUEST_INTERVAL` to change). Dependency-free Python stdlib, one file.
- Before its first write, the plugin backs up your settings to `~/.claude/sidequest/settings.backup.json`. `/sidequest:off` restores the spinner keys from that backup.

## FAQ

**Does it phone home?** No. There is no server. Facts are generated locally by your Claude and stored on your disk.

**Does it cost tokens?** Only when generating a pool (~once per topic). Rotation is a local script, zero tokens.

**Will facts be true?** The generation prompt demands verifiable facts and bans inventing them. It's still an LLM — treat the spinner as delight, not as a citation source.

**Why are facts short?** The spinner is one terminal line. Facts longer than 64 characters are dropped by the rotator, so the generator aims for ≤ 60.

**How do I uninstall completely?**

```
/sidequest:off
/plugin uninstall sidequest
rm -rf ~/.claude/sidequest
```

## License

MIT
